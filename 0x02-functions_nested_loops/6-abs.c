#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @num: integer to be verified
 * Return: the absolute value of the integer
*/
int _abs(int num)
{
	if (num < 0)
	{
		num = num * -1;
		return (num);
	}

	else
	{
		return (num);
	}
}
